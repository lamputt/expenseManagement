<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Màu nền (có thể bỏ nếu không cần) -->
    <solid android:color="@color/color_transfer" />

    <!-- Bo góc -->
    <corners android:radius="16dp" />

    <!-- Đường viền -->
    <stroke
        android:width="2dp"
        android:color="@color/color_transfer" />

</shape>

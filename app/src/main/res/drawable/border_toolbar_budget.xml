<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Màu nền (có thể bỏ nếu không cần) -->
    <solid android:color="#FCAC12" />

    <!-- Bo góc -->
    <corners android:radius="12dp" />

    <!-- Đường viền -->
    <stroke
        android:width="1dp"
        android:color="#FCAC12" />
</shape>

<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Màu nền (có thể bỏ nếu không cần) -->
    <solid android:color="@color/second_background_button" />

    <!-- Bo góc -->
    <corners android:radius="8dp" />

    <!-- Đường viền -->
    <stroke
        android:width="1dp"
        android:color="@color/second_background_button" />
    <padding
        android:bottom="8dp"
        android:right="16dp"
        android:left="16dp"
        android:top="8dp"
        />
</shape>

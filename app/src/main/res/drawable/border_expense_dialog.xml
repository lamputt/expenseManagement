<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Màu nền (có thể bỏ nếu không cần) -->
    <solid android:color="@color/color_expense" />

    <!-- Bo góc -->
    <corners android:radius="16dp" />

    <!-- Đường viền -->
    <stroke
        android:width="1dp"
        android:color="@color/color_expense" />

</shape>
